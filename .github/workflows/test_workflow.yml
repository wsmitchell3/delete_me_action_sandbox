name: test_workflow

on:
  workflow-dispatch:
    inputs:
      environment:
        description: "Environment to deploy to"
        type: environment
        required: true

jobs:
  hello:
    runs-on: ubuntu-latest
